import styles from './Credentials.module.scss';

const Credentials = () => {
  return (
    <section className={styles.container} data-testid="credentials">
      <h2 className={styles.title}>Credentials</h2>
    </section>
  )
}

export default Credentials;